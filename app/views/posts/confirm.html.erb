<h1>下書き一覧</h1>

<% @drafts.each do |post| %>
  <div>
    <p><%= link_to post.name, post_path(post) %></p>
    <p><%= truncate(post.text, length: 100) %></p>
  </div>
<% end %>

<%= link_to "新規投稿ページへ戻る", new_post_path %>
