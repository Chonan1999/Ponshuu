<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Ponshuu" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload", media: "all" %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <!-- ヘッダー（ログイン時のみ表示） -->
    <% if user_signed_in? %>
      <header class="main-header">
        <nav class="navbar">
          <a href="<%= root_path %>" class="navbar-brand">Ponshuu</a>
          <ul class="navbar-menu">
            <li><%= link_to "マイページ", user_path(current_user), class: "text-white nav-link" %></li>
            <li><a href="<%= posts_path %>">みんなの投稿一覧</a></li>
            <li><a href="<%= new_post_path %>">投稿する</a></li>
            <li><%= link_to "ユーザ一覧", users_path, class: "text-white nav-link" %></li>
            <li><%= link_to "ログアウト", destroy_user_session_path, data: { turbo_method: :delete }, class: "logout-link" %></li>
          </ul>
        </nav>
      </header>
    <% end %>

    <!-- メインコンテンツ -->
    <main class="main-content">
      <%= yield %>
    </main>

    <!-- フッター -->
    <footer class="main-footer">
      <p>&copy; 2024 Ponshuu - 日本酒交流アプリ</p>
    </footer>
  </body>
</html>
